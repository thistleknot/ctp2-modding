// mom_spellbook_death.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Paged spellbook for DEATH sphere. Each page has 5 arms:
//   arm 0 = Close, arms 1-4 = spells or navigation (Prev/Next).

// ===== DEATH SPELLBOOK: 23 spells across 11 page(s) =====

// Page 1 of 11 (death)
alertbox 'MomSpellPage_death_1' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_1);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Skeletons (10 mana)
    Button(ID_MOM_SPELL_89) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 89);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 2: Dark Rituals (10 mana)
    Button(ID_MOM_SPELL_90) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 90);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Cloak of Fear (10 mana)
    Button(ID_MOM_SPELL_91) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 91);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
}

// Page 2 of 11 (death)
alertbox 'MomSpellPage_death_2' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_2);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 1
    }
    // Arm 2: Ghouls (10 mana)
    Button(ID_MOM_SPELL_92) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 92);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Drain Power (10 mana)
    Button(ID_MOM_SPELL_93) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 93);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
}

// Page 3 of 11 (death)
alertbox 'MomSpellPage_death_3' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_3);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
    // Arm 2: Lycanthropy (18 mana)
    Button(ID_MOM_SPELL_94) {
        if (MomMagicCur[player[0]] >= 18) {
            MomCastSpell(player[0], 94);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Black Channels (10 mana)
    Button(ID_MOM_SPELL_95) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 95);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
}

// Page 4 of 11 (death)
alertbox 'MomSpellPage_death_4' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_4);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
    // Arm 2: Night Stalker (25 mana)
    Button(ID_MOM_SPELL_96) {
        if (MomMagicCur[player[0]] >= 25) {
            MomCastSpell(player[0], 96);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Subversion (10 mana)
    Button(ID_MOM_SPELL_97) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 97);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
}

// Page 5 of 11 (death)
alertbox 'MomSpellPage_death_5' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_5);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
    // Arm 2: Wall of Darkness (20 mana)
    Button(ID_MOM_SPELL_98) {
        if (MomMagicCur[player[0]] >= 20) {
            MomCastSpell(player[0], 98);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Shadow Demons (32 mana)
    Button(ID_MOM_SPELL_99) {
        if (MomMagicCur[player[0]] >= 32) {
            MomCastSpell(player[0], 99);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
}

// Page 6 of 11 (death)
alertbox 'MomSpellPage_death_6' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_6);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
    // Arm 2: Wraith Form (22 mana)
    Button(ID_MOM_SPELL_100) {
        if (MomMagicCur[player[0]] >= 22) {
            MomCastSpell(player[0], 100);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Wraiths (75 mana)
    Button(ID_MOM_SPELL_101) {
        if (MomMagicCur[player[0]] >= 75) {
            MomCastSpell(player[0], 101);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
}

// Page 7 of 11 (death)
alertbox 'MomSpellPage_death_7' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_7);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
    // Arm 2: Cloud of Shadow (22 mana)
    Button(ID_MOM_SPELL_102) {
        if (MomMagicCur[player[0]] >= 22) {
            MomCastSpell(player[0], 102);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Warp Node (11 mana)
    Button(ID_MOM_SPELL_103) {
        if (MomMagicCur[player[0]] >= 11) {
            MomCastSpell(player[0], 103);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
}

// Page 8 of 11 (death)
alertbox 'MomSpellPage_death_8' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_8);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
    // Arm 2: Black Wind (30 mana)
    Button(ID_MOM_SPELL_104) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 104);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Zombie Mastery (120 mana)
    Button(ID_MOM_SPELL_105) {
        if (MomMagicCur[player[0]] >= 120) {
            MomCastSpell(player[0], 105);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
}

// Page 9 of 11 (death)
alertbox 'MomSpellPage_death_9' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_9);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
    // Arm 2: Cruel Unminding (50 mana)
    Button(ID_MOM_SPELL_106) {
        if (MomMagicCur[player[0]] >= 50) {
            MomCastSpell(player[0], 106);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Death Knights (120 mana)
    Button(ID_MOM_SPELL_107) {
        if (MomMagicCur[player[0]] >= 120) {
            MomCastSpell(player[0], 107);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
}

// Page 10 of 11 (death)
alertbox 'MomSpellPage_death_10' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_10);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
    // Arm 2: Eternal Night (180 mana)
    Button(ID_MOM_SPELL_108) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 108);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Evil Omens (180 mana)
    Button(ID_MOM_SPELL_109) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 109);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
}

// Page 11 of 11 (death)
alertbox 'MomSpellPage_death_11' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_DEATH_11);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
    // Arm 2: Death Wish (100 mana)
    Button(ID_MOM_SPELL_110) {
        if (MomMagicCur[player[0]] >= 100) {
            MomCastSpell(player[0], 110);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Demon Lord (180 mana)
    Button(ID_MOM_SPELL_111) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 111);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
