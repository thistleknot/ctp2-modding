// mom_spellbook_sorcery.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Paged spellbook for SORCERY sphere. Each page has 5 arms:
//   arm 0 = Close, arms 1-4 = spells or navigation (Prev/Next).

// ===== SORCERY SPELLBOOK: 26 spells across 12 page(s) =====

// Page 1 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_1' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_1);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Resist Magic (10 mana)
    Button(ID_MOM_SPELL_63) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 63);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 2: Floating Island (10 mana)
    Button(ID_MOM_SPELL_64) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 64);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Guardian Wind (10 mana)
    Button(ID_MOM_SPELL_65) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 65);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
}

// Page 2 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_2' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_2);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 1
    }
    // Arm 2: Word of Recall (10 mana)
    Button(ID_MOM_SPELL_66) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 66);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Nagas (10 mana)
    Button(ID_MOM_SPELL_67) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 67);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
}

// Page 3 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_3' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_3);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
    // Arm 2: Disenchant True (10 mana)
    Button(ID_MOM_SPELL_68) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 68);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Spell Lock (10 mana)
    Button(ID_MOM_SPELL_69) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 69);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
}

// Page 4 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_4' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_4);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
    // Arm 2: Enchant Road (10 mana)
    Button(ID_MOM_SPELL_70) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 70);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Flight (12 mana)
    Button(ID_MOM_SPELL_71) {
        if (MomMagicCur[player[0]] >= 12) {
            MomCastSpell(player[0], 71);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
}

// Page 5 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_5' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_5);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
    // Arm 2: Wind Mastery (40 mana)
    Button(ID_MOM_SPELL_72) {
        if (MomMagicCur[player[0]] >= 40) {
            MomCastSpell(player[0], 72);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Spell Blast (10 mana)
    Button(ID_MOM_SPELL_73) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 73);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
}

// Page 6 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_6' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_6);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
    // Arm 2: Aura of Majesty (40 mana)
    Button(ID_MOM_SPELL_74) {
        if (MomMagicCur[player[0]] >= 40) {
            MomCastSpell(player[0], 74);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Disjunction True (30 mana)
    Button(ID_MOM_SPELL_75) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 75);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
}

// Page 7 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_7' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_7);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
    // Arm 2: Invisibility (26 mana)
    Button(ID_MOM_SPELL_76) {
        if (MomMagicCur[player[0]] >= 26) {
            MomCastSpell(player[0], 76);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Wind Walking (37 mana)
    Button(ID_MOM_SPELL_77) {
        if (MomMagicCur[player[0]] >= 37) {
            MomCastSpell(player[0], 77);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
}

// Page 8 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_8' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_8);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
    // Arm 2: Storm Giant (75 mana)
    Button(ID_MOM_SPELL_78) {
        if (MomMagicCur[player[0]] >= 75) {
            MomCastSpell(player[0], 78);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Stasis (37 mana)
    Button(ID_MOM_SPELL_79) {
        if (MomMagicCur[player[0]] >= 37) {
            MomCastSpell(player[0], 79);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
}

// Page 9 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_9' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_9);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
    // Arm 2: Magic Immunity (37 mana)
    Button(ID_MOM_SPELL_80) {
        if (MomMagicCur[player[0]] >= 37) {
            MomCastSpell(player[0], 80);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Djinn (130 mana)
    Button(ID_MOM_SPELL_81) {
        if (MomMagicCur[player[0]] >= 130) {
            MomCastSpell(player[0], 81);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
}

// Page 10 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_10' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_10);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
    // Arm 2: Spell Ward (70 mana)
    Button(ID_MOM_SPELL_82) {
        if (MomMagicCur[player[0]] >= 70) {
            MomCastSpell(player[0], 82);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Great Unsummoning (180 mana)
    Button(ID_MOM_SPELL_83) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 83);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
}

// Page 11 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_11' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_11);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
    // Arm 2: Spell Binding (180 mana)
    Button(ID_MOM_SPELL_84) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 84);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Flying Fortress (100 mana)
    Button(ID_MOM_SPELL_85) {
        if (MomMagicCur[player[0]] >= 100) {
            MomCastSpell(player[0], 85);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 12
    }
}

// Page 12 of 12 (sorcery)
alertbox 'MomSpellPage_sorcery_12' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_SORCERY_12);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
    // Arm 2: Sky Drake (180 mana)
    Button(ID_MOM_SPELL_86) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 86);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Suppress Magic (180 mana)
    Button(ID_MOM_SPELL_87) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 87);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Time Stop (180 mana)
    Button(ID_MOM_SPELL_88) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 88);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
