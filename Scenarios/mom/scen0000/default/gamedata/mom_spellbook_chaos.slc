// mom_spellbook_chaos.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Paged spellbook for CHAOS sphere. Each page has 5 arms:
//   arm 0 = Close, arms 1-4 = spells or navigation (Prev/Next).

// ===== CHAOS SPELLBOOK: 23 spells across 11 page(s) =====

// Page 1 of 11 (chaos)
alertbox 'MomSpellPage_chaos_1' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_1);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Hell Hounds (10 mana)
    Button(ID_MOM_SPELL_112) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 112);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 2: Corruption (10 mana)
    Button(ID_MOM_SPELL_113) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 113);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Eldritch Weapon (10 mana)
    Button(ID_MOM_SPELL_114) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 114);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
}

// Page 2 of 11 (chaos)
alertbox 'MomSpellPage_chaos_2' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_2);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 1
    }
    // Arm 2: Wall of Fire (10 mana)
    Button(ID_MOM_SPELL_115) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 115);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Fire Giant (15 mana)
    Button(ID_MOM_SPELL_116) {
        if (MomMagicCur[player[0]] >= 15) {
            MomCastSpell(player[0], 116);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
}

// Page 3 of 11 (chaos)
alertbox 'MomSpellPage_chaos_3' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_3);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
    // Arm 2: Chaos Channels (10 mana)
    Button(ID_MOM_SPELL_117) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 117);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Flame Blade (12 mana)
    Button(ID_MOM_SPELL_118) {
        if (MomMagicCur[player[0]] >= 12) {
            MomCastSpell(player[0], 118);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
}

// Page 4 of 11 (chaos)
alertbox 'MomSpellPage_chaos_4' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_4);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
    // Arm 2: Gargoyles (20 mana)
    Button(ID_MOM_SPELL_119) {
        if (MomMagicCur[player[0]] >= 20) {
            MomCastSpell(player[0], 119);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Doom Bat (30 mana)
    Button(ID_MOM_SPELL_120) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 120);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
}

// Page 5 of 11 (chaos)
alertbox 'MomSpellPage_chaos_5' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_5);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
    // Arm 2: Raise Volcano (20 mana)
    Button(ID_MOM_SPELL_121) {
        if (MomMagicCur[player[0]] >= 20) {
            MomCastSpell(player[0], 121);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Immolation (15 mana)
    Button(ID_MOM_SPELL_122) {
        if (MomMagicCur[player[0]] >= 15) {
            MomCastSpell(player[0], 122);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
}

// Page 6 of 11 (chaos)
alertbox 'MomSpellPage_chaos_6' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_6);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
    // Arm 2: Chimeras (35 mana)
    Button(ID_MOM_SPELL_123) {
        if (MomMagicCur[player[0]] >= 35) {
            MomCastSpell(player[0], 123);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Chaos Spawn (75 mana)
    Button(ID_MOM_SPELL_124) {
        if (MomMagicCur[player[0]] >= 75) {
            MomCastSpell(player[0], 124);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
}

// Page 7 of 11 (chaos)
alertbox 'MomSpellPage_chaos_7' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_7);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
    // Arm 2: Efreet (82 mana)
    Button(ID_MOM_SPELL_125) {
        if (MomMagicCur[player[0]] >= 82) {
            MomCastSpell(player[0], 125);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Fire Storm (37 mana)
    Button(ID_MOM_SPELL_126) {
        if (MomMagicCur[player[0]] >= 37) {
            MomCastSpell(player[0], 126);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
}

// Page 8 of 11 (chaos)
alertbox 'MomSpellPage_chaos_8' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_8);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
    // Arm 2: Hydra (130 mana)
    Button(ID_MOM_SPELL_127) {
        if (MomMagicCur[player[0]] >= 130) {
            MomCastSpell(player[0], 127);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Meteor Storm (180 mana)
    Button(ID_MOM_SPELL_128) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 128);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
}

// Page 9 of 11 (chaos)
alertbox 'MomSpellPage_chaos_9' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_9);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
    // Arm 2: Great Wasting (180 mana)
    Button(ID_MOM_SPELL_129) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 129);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Chaos Surge (180 mana)
    Button(ID_MOM_SPELL_130) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 130);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
}

// Page 10 of 11 (chaos)
alertbox 'MomSpellPage_chaos_10' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_10);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
    // Arm 2: Doom Mastery (180 mana)
    Button(ID_MOM_SPELL_131) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 131);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Great Drake (180 mana)
    Button(ID_MOM_SPELL_132) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 132);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
}

// Page 11 of 11 (chaos)
alertbox 'MomSpellPage_chaos_11' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_CHAOS_11);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
    // Arm 2: Call the Void (100 mana)
    Button(ID_MOM_SPELL_133) {
        if (MomMagicCur[player[0]] >= 100) {
            MomCastSpell(player[0], 133);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Armageddon (180 mana)
    Button(ID_MOM_SPELL_134) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 134);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
