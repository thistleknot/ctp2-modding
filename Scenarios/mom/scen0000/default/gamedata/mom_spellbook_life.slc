// mom_spellbook_life.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Paged spellbook for LIFE sphere. Each page has 5 arms:
//   arm 0 = Close, arms 1-4 = spells or navigation (Prev/Next).

// ===== LIFE SPELLBOOK: 31 spells across 15 page(s) =====

// Page 1 of 15 (life)
alertbox 'MomSpellPage_life_1' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_1);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Bless (10 mana)
    Button(ID_MOM_SPELL_0) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 0);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 2: Endurance (10 mana)
    Button(ID_MOM_SPELL_1) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 1);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Holy Weapon (10 mana)
    Button(ID_MOM_SPELL_2) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 2);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
}

// Page 2 of 15 (life)
alertbox 'MomSpellPage_life_2' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_2);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 1
    }
    // Arm 2: Holy Armor (10 mana)
    Button(ID_MOM_SPELL_3) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 3);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Just Cause (10 mana)
    Button(ID_MOM_SPELL_4) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 4);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
}

// Page 3 of 15 (life)
alertbox 'MomSpellPage_life_3' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_3);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
    // Arm 2: Guardian Spirit (10 mana)
    Button(ID_MOM_SPELL_5) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 5);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Heroism (10 mana)
    Button(ID_MOM_SPELL_6) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 6);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
}

// Page 4 of 15 (life)
alertbox 'MomSpellPage_life_4' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_4);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
    // Arm 2: True Sight (10 mana)
    Button(ID_MOM_SPELL_7) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 7);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Plane Shift (12 mana)
    Button(ID_MOM_SPELL_8) {
        if (MomMagicCur[player[0]] >= 12) {
            MomCastSpell(player[0], 8);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
}

// Page 5 of 15 (life)
alertbox 'MomSpellPage_life_5' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_5);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
    // Arm 2: Resurrection (25 mana)
    Button(ID_MOM_SPELL_9) {
        if (MomMagicCur[player[0]] >= 25) {
            MomCastSpell(player[0], 9);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Planar Seal (50 mana)
    Button(ID_MOM_SPELL_10) {
        if (MomMagicCur[player[0]] >= 50) {
            MomCastSpell(player[0], 10);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
}

// Page 6 of 15 (life)
alertbox 'MomSpellPage_life_6' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_6);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
    // Arm 2: Unicorns (25 mana)
    Button(ID_MOM_SPELL_11) {
        if (MomMagicCur[player[0]] >= 25) {
            MomCastSpell(player[0], 11);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Planar Travel (15 mana)
    Button(ID_MOM_SPELL_12) {
        if (MomMagicCur[player[0]] >= 15) {
            MomCastSpell(player[0], 12);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
}

// Page 7 of 15 (life)
alertbox 'MomSpellPage_life_7' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_7);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
    // Arm 2: Heavenly Light (15 mana)
    Button(ID_MOM_SPELL_13) {
        if (MomMagicCur[player[0]] >= 15) {
            MomCastSpell(player[0], 13);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Lionheart (30 mana)
    Button(ID_MOM_SPELL_14) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 14);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
}

// Page 8 of 15 (life)
alertbox 'MomSpellPage_life_8' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_8);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
    // Arm 2: Incarnation (75 mana)
    Button(ID_MOM_SPELL_15) {
        if (MomMagicCur[player[0]] >= 75) {
            MomCastSpell(player[0], 15);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Invulnerability (30 mana)
    Button(ID_MOM_SPELL_16) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 16);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
}

// Page 9 of 15 (life)
alertbox 'MomSpellPage_life_9' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_9);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
    // Arm 2: Righteousness (30 mana)
    Button(ID_MOM_SPELL_17) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 17);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Prosperity (37 mana)
    Button(ID_MOM_SPELL_18) {
        if (MomMagicCur[player[0]] >= 37) {
            MomCastSpell(player[0], 18);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
}

// Page 10 of 15 (life)
alertbox 'MomSpellPage_life_10' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_10);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
    // Arm 2: Altar of Battle (45 mana)
    Button(ID_MOM_SPELL_19) {
        if (MomMagicCur[player[0]] >= 45) {
            MomCastSpell(player[0], 19);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Angel (82 mana)
    Button(ID_MOM_SPELL_20) {
        if (MomMagicCur[player[0]] >= 82) {
            MomCastSpell(player[0], 20);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
}

// Page 11 of 15 (life)
alertbox 'MomSpellPage_life_11' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_11);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
    // Arm 2: Stream of Life (45 mana)
    Button(ID_MOM_SPELL_21) {
        if (MomMagicCur[player[0]] >= 45) {
            MomCastSpell(player[0], 21);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Inspirations (70 mana)
    Button(ID_MOM_SPELL_22) {
        if (MomMagicCur[player[0]] >= 70) {
            MomCastSpell(player[0], 22);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 12
    }
}

// Page 12 of 15 (life)
alertbox 'MomSpellPage_life_12' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_12);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
    // Arm 2: Astral Gate (70 mana)
    Button(ID_MOM_SPELL_23) {
        if (MomMagicCur[player[0]] >= 70) {
            MomCastSpell(player[0], 23);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Holy Arms (180 mana)
    Button(ID_MOM_SPELL_24) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 24);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 13
    }
}

// Page 13 of 15 (life)
alertbox 'MomSpellPage_life_13' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_13);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 12
    }
    // Arm 2: Consecration (80 mana)
    Button(ID_MOM_SPELL_25) {
        if (MomMagicCur[player[0]] >= 80) {
            MomCastSpell(player[0], 25);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Life Force (180 mana)
    Button(ID_MOM_SPELL_26) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 26);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 14
    }
}

// Page 14 of 15 (life)
alertbox 'MomSpellPage_life_14' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_14);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 13
    }
    // Arm 2: Tranquility (180 mana)
    Button(ID_MOM_SPELL_27) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 27);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Crusade (180 mana)
    Button(ID_MOM_SPELL_28) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 28);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 15
    }
}

// Page 15 of 15 (life)
alertbox 'MomSpellPage_life_15' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_LIFE_15);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 14
    }
    // Arm 2: Arch Angel (180 mana)
    Button(ID_MOM_SPELL_29) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 29);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Charm of Life (180 mana)
    Button(ID_MOM_SPELL_30) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 30);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
