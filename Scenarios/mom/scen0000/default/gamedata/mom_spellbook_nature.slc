// mom_spellbook_nature.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Paged spellbook for NATURE sphere. Each page has 5 arms:
//   arm 0 = Close, arms 1-4 = spells or navigation (Prev/Next).

// ===== NATURE SPELLBOOK: 32 spells across 15 page(s) =====

// Page 1 of 15 (nature)
alertbox 'MomSpellPage_nature_1' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_1);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Resist Elements (10 mana)
    Button(ID_MOM_SPELL_31) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 31);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 2: Wall of Stone (10 mana)
    Button(ID_MOM_SPELL_32) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 32);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Giant Strength (10 mana)
    Button(ID_MOM_SPELL_33) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 33);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
}

// Page 2 of 15 (nature)
alertbox 'MomSpellPage_nature_2' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_2);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 1
    }
    // Arm 2: War Bears (10 mana)
    Button(ID_MOM_SPELL_34) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 34);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Stone Skin (10 mana)
    Button(ID_MOM_SPELL_35) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 35);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
}

// Page 3 of 15 (nature)
alertbox 'MomSpellPage_nature_3' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_3);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 2
    }
    // Arm 2: Water Walking (10 mana)
    Button(ID_MOM_SPELL_36) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 36);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Sprites (10 mana)
    Button(ID_MOM_SPELL_37) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 37);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
}

// Page 4 of 15 (nature)
alertbox 'MomSpellPage_nature_4' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_4);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 3
    }
    // Arm 2: Earth Lore (10 mana)
    Button(ID_MOM_SPELL_38) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 38);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Nature's Eye (10 mana)
    Button(ID_MOM_SPELL_39) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 39);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
}

// Page 5 of 15 (nature)
alertbox 'MomSpellPage_nature_5' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_5);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 4
    }
    // Arm 2: Giant Spiders (20 mana)
    Button(ID_MOM_SPELL_40) {
        if (MomMagicCur[player[0]] >= 20) {
            MomCastSpell(player[0], 40);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Change Terrain (10 mana)
    Button(ID_MOM_SPELL_41) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 41);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
}

// Page 6 of 15 (nature)
alertbox 'MomSpellPage_nature_6' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_6);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 5
    }
    // Arm 2: Path Finding (10 mana)
    Button(ID_MOM_SPELL_42) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 42);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Cockatrices (27 mana)
    Button(ID_MOM_SPELL_43) {
        if (MomMagicCur[player[0]] >= 27) {
            MomCastSpell(player[0], 43);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
}

// Page 7 of 15 (nature)
alertbox 'MomSpellPage_nature_7' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_7);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 6
    }
    // Arm 2: Transmute (10 mana)
    Button(ID_MOM_SPELL_44) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 44);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Nature's Cures (10 mana)
    Button(ID_MOM_SPELL_45) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 45);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
}

// Page 8 of 15 (nature)
alertbox 'MomSpellPage_nature_8' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_8);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 7
    }
    // Arm 2: Basilisk (32 mana)
    Button(ID_MOM_SPELL_46) {
        if (MomMagicCur[player[0]] >= 32) {
            MomCastSpell(player[0], 46);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Elemental Armor (26 mana)
    Button(ID_MOM_SPELL_47) {
        if (MomMagicCur[player[0]] >= 26) {
            MomCastSpell(player[0], 47);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
}

// Page 9 of 15 (nature)
alertbox 'MomSpellPage_nature_9' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_9);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 8
    }
    // Arm 2: Stone Giant (67 mana)
    Button(ID_MOM_SPELL_48) {
        if (MomMagicCur[player[0]] >= 67) {
            MomCastSpell(player[0], 48);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Iron Skin (30 mana)
    Button(ID_MOM_SPELL_49) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 49);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
}

// Page 10 of 15 (nature)
alertbox 'MomSpellPage_nature_10' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_10);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 9
    }
    // Arm 2: Ice Storm (30 mana)
    Button(ID_MOM_SPELL_50) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 50);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Earthquake (30 mana)
    Button(ID_MOM_SPELL_51) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 51);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
}

// Page 11 of 15 (nature)
alertbox 'MomSpellPage_nature_11' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_11);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 10
    }
    // Arm 2: Gorgons (90 mana)
    Button(ID_MOM_SPELL_52) {
        if (MomMagicCur[player[0]] >= 90) {
            MomCastSpell(player[0], 52);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Move Fortress (30 mana)
    Button(ID_MOM_SPELL_53) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 53);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 12
    }
}

// Page 12 of 15 (nature)
alertbox 'MomSpellPage_nature_12' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_12);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 11
    }
    // Arm 2: Gaia's Blessing (45 mana)
    Button(ID_MOM_SPELL_54) {
        if (MomMagicCur[player[0]] >= 45) {
            MomCastSpell(player[0], 54);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Regeneration (60 mana)
    Button(ID_MOM_SPELL_55) {
        if (MomMagicCur[player[0]] >= 60) {
            MomCastSpell(player[0], 55);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 13
    }
}

// Page 13 of 15 (nature)
alertbox 'MomSpellPage_nature_13' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_13);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 12
    }
    // Arm 2: Behemoth (140 mana)
    Button(ID_MOM_SPELL_56) {
        if (MomMagicCur[player[0]] >= 140) {
            MomCastSpell(player[0], 56);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Nature Awareness (160 mana)
    Button(ID_MOM_SPELL_57) {
        if (MomMagicCur[player[0]] >= 160) {
            MomCastSpell(player[0], 57);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 14
    }
}

// Page 14 of 15 (nature)
alertbox 'MomSpellPage_nature_14' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_14);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 13
    }
    // Arm 2: Colossus (160 mana)
    Button(ID_MOM_SPELL_58) {
        if (MomMagicCur[player[0]] >= 160) {
            MomCastSpell(player[0], 58);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Earth Gate (50 mana)
    Button(ID_MOM_SPELL_59) {
        if (MomMagicCur[player[0]] >= 50) {
            MomCastSpell(player[0], 59);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Next Page
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        // TODO Phase 3: navigate to page 15
    }
}

// Page 15 of 15 (nature)
alertbox 'MomSpellPage_nature_15' {
    Show();
    Text(ID_MOM_SPELLBOOK_TITLE_NATURE_15);

    // Arm 0: Close
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Arm 1: Previous Page
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        // TODO Phase 3: navigate to page 14
    }
    // Arm 2: Herb Mastery (180 mana)
    Button(ID_MOM_SPELL_60) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 60);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 3: Great Wyrm (180 mana)
    Button(ID_MOM_SPELL_61) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 61);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Arm 4: Nature's Wrath (180 mana)
    Button(ID_MOM_SPELL_62) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 62);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
